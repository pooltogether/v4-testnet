const DRAW_BUFFER_CARDINALITY = 255; 
const PRIZE_DISTRIBUTION_BUFFER_CARDINALITY = 180;
const BEACON_PERIOD_SECONDS = 60 * 10; // 10 minutes
const END_TIMESTAMP_OFFSET = 60 // 60 seconds
const TOKEN_DECIMALS = 6;
const VALIDITY_DURATION = BEACON_PERIOD_SECONDS * 10 // valid for 10 periods

module.exports = {
  DRAW_BUFFER_CARDINALITY,
  PRIZE_DISTRIBUTION_BUFFER_CARDINALITY,
  BEACON_PERIOD_SECONDS,
  END_TIMESTAMP_OFFSET,
  VALIDITY_DURATION,
  TOKEN_DECIMALS
}
